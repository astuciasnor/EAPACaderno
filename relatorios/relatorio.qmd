---
title: "Densidade de estocagem e crescimento da tilápia em tanques-rede"
subtitle: "Relatório de análise — EAPACaderno"
author:
  - name: "Evaldo Martins da Silva"
    affiliation: "Instituto de Estudos Costeiros; Universidade Federal do Pará"
date: today
date-format: "D [de] MMMM [de] YYYY"
lang: pt-BR
abstract-title: "Resumo"
abstract: |
  Avaliou-se o efeito da densidade de estocagem (50, 100, 150 e 200 peixes/m³)
  sobre o ganho de peso da tilápia-do-nilo em tanques-rede, em delineamento
  inteiramente casualizado com seis tanques por tratamento, ao longo de oito
  semanas. O ganho de peso caiu com a densidade; as duas menores não diferiram
  entre si, e a perda concentrou-se a partir de 150 peixes/m³. Os dados são
  fictícios e servem para ilustrar o método.
bibliography: referencias.bib                        # as obras citadas no texto ([@chave])
csl: abnt.csl                                        # estilo das citações e da lista: ABNT (padrão)
# csl: apa.csl                                       # ... ou APA: troque a linha acima por esta
crossref:
  title-delim: " – "                                 # "Tabela 1 – ..." em vez de "Tabela 1: ...", como na ABNT
format:
  # 1. O Word: o relatório para o leitor. Só texto, tabelas e figuras.
  docx:
    reference-doc: custom-reference.docx   # modelo de página com cara de artigo (o mesmo da CatalyseR)
    toc: false                              # artigo não tem sumário
    number-sections: true
    fig-width: 4.9                          # 12,5 cm: sobra margem dos dois lados, como em artigo
    fig-height: 3.0                         # 7,6 cm
    fig-dpi: 300
    fig-align: center
  # 2. O HTML: o caderno do pesquisador. O mesmo documento, com o código
  #    dobrado (clique em "Código" para abrir) e a exploração à mostra.
  html:
    theme: [cosmo, ocean.scss]              # cores e fontes do ecossistema EAPA (Ocean Gradient)
    title-block-banner: "#0F3B5F"           # faixa azul-marinho com título, autor e data
    title-block-banner-color: "white"
    toc: true
    toc-location: left                      # índice à esquerda; o ocean.scss o sobe para o alto, ao lado do Resumo
    toc-depth: 2
    toc-title: "Neste caderno"
    number-sections: true
    embed-resources: true                   # um único arquivo .html, fácil de enviar
    echo: true                              # aqui o código aparece...
    code-fold: true                         # ... dobrado, para não atrapalhar a leitura
    code-summary: "Código"
    code-tools: true                        # menu no topo: mostrar/esconder todo o código
    code-copy: true                         # botão de copiar em cada bloco
    df-print: kable                         # tabelas exploratórias como tabela, não como texto de console
    fig-width: 5.2                          # figuras menores que no Word: a tela é mais estreita que a página
    fig-height: 3.2
    fig-align: center
    smooth-scroll: true
execute:
  echo: false                  # no Word, o leitor vê resultados, não código
  warning: false
  message: false
editor_options: 
  chunk_output_type: console
---

```{=html}
<!--
  Este documento é o projeto inteiro: os dados entram, são preparados,
  explorados e analisados aqui dentro, e o texto é escrito em cima dos
  resultados. Não há scripts separados. Para refazer tudo do zero, reinicie
  o R (Ctrl+Shift+F10) e clique em Render.

  Os chunks têm nomes que dizem o que fazem: pacotes, importar, tratar,
  explora-*, analisar, tbl-*, fig-*. Rode qualquer um deles no RStudio
  (Ctrl+Shift+Enter) e a saída aparece no console, como sempre. O que vai
  para o Word é só o que este arquivo manda: tabelas, figuras e o texto.
-->
```

```{r}
#| label: instalar
#| eval: false
# Rode este chunk UMA vez, em computador novo (Ctrl+Shift+Enter). Ele instala
# o que faltar e não roda no Render (eval: false).
pacotes <- c("here", "readxl", "readr", "dplyr", "tidyr", "ggplot2",
             "car", "multcompView", "flextable")
faltando <- pacotes[!pacotes %in% rownames(installed.packages())]
if (length(faltando)) install.packages(faltando)
```

```{r}
#| label: pacotes
#| include: false
# Os pacotes que o documento usa. here() monta os caminhos a partir da raiz
# do projeto (onde está o .Rproj), então o mesmo arquivo roda em qualquer
# computador. As funções próprias (resumos, tema, tabelas) vêm de R/funcoes.R.
library(here)
library(readxl)        # ler a planilha
library(readr)         # write_csv2(): csv no padrão brasileiro (; e vírgula)
library(dplyr)
library(tidyr)
library(ggplot2)
library(car)           # leveneTest()
library(multcompView)  # multcompLetters(): letras a partir do Tukey
library(flextable)     # tabelas no Word; o visual vem de flextable_ocean()

source(here("R", "funcoes.R"))

# Três casas decimais nas tabelas exploratórias do caderno. Sem isto, o R
# imprime sete (149.11667), o que polui a leitura. As tabelas do relatório
# não dependem disto: nelas, fmt() e formatar_p() decidem cada número.
options(digits = 3)
```

# Introdução

Este relatório é o exemplo que acompanha o EAPACaderno. O experimento (fictício) avaliou o efeito da densidade de estocagem sobre o crescimento da tilápia-do-nilo em tanques-rede, ao longo de oito semanas. Foram testadas quatro densidades (50, 100, 150 e 200 peixes/m³) em delineamento inteiramente casualizado, com seis tanques por tratamento. O tanque-rede foi a unidade experimental.

A pergunta é simples: aumentar a densidade reduz o ganho de peso? E, se reduz, a partir de qual densidade a diferença aparece?

# Material e métodos

## Os dados

Os dados foram registrados em planilha de campo (`dados/brutos/crescimento_tilapia.xlsx`), com duas abas: `biometria` (uma linha por tanque) e `agua` (uma linha por tanque e semana). A planilha é somente-leitura: nunca a editamos à mão. Toda correção é feita em código, no chunk `tratar`, com um comentário dizendo o porquê.

```{r}
#| label: importar
#| output: false
# Ler a planilha como ela veio, sem mexer em nada. A aba "biometria" tem duas
# linhas de título antes do cabeçalho, por isso o skip = 3.
arquivo <- here("dados", "brutos", "crescimento_tilapia.xlsx")
excel_sheets(arquivo)              # vale sempre olhar as abas antes de ler

biometria_bruta <- read_excel(arquivo, sheet = "biometria", skip = 3)
agua_bruta      <- read_excel(arquivo, sheet = "agua")

# Primeira olhada. Repare: "Peso final (g)" veio como texto (chr) porque
# alguém digitou um valor com vírgula. O chunk seguinte resolve.
str(biometria_bruta)
str(agua_bruta)
```

## Preparo dos dados

Aqui a planilha de campo vira uma base de análise: nomes de coluna curtos, cada coluna com o tipo certo, valores faltantes como `NA` e as variáveis derivadas calculadas uma vez só. Se alguém perguntar "por que o tanque 19 está sem sobrevivência?", a resposta está no chunk abaixo.

```{r}
#| label: tratar
#| output: false
# 1. Renomear à mão: deixa explícito o "de-para" entre a planilha e a base.
#    (Para planilhas grandes, janitor::clean_names() faz isso automaticamente.)
biometria <- biometria_bruta |>
  rename(
    tanque        = `Tanque`,
    densidade     = `Densidade (peixes/m³)`,
    peso_inicial  = `Peso inicial (g)`,
    peso_final    = `Peso final (g)`,
    comprimento   = `Comprimento final (cm)`,
    sobrevivencia = `Sobrevivência (%)`,
    observacoes   = `Observações`
  )

agua <- agua_bruta |>
  rename(
    tanque      = `Tanque`,
    semana      = `Semana`,
    temperatura = `Temperatura (°C)`,
    oxigenio    = `Oxigênio dissolvido (mg/L)`,
    ph          = `pH`
  )

# 2. Acertar os tipos e os NA
biometria <- biometria |>
  mutate(
    # Um "168,3" digitado como texto fez a coluna inteira virar texto.
    # Trocamos a vírgula por ponto e convertemos para número.
    peso_final    = as.numeric(sub(",", ".", peso_final, fixed = TRUE)),

    # O "-" que o anotador usou vira NA, o padrão do R para faltante.
    # Os dois NA da base têm motivo conhecido, e vale registrá-lo aqui:
    # T14 sem comprimento (ictiômetro quebrado) e T19 sem sobrevivência
    # (contagem não realizada). Ver também a aba "leia-me" da planilha.
    sobrevivencia = as.numeric(na_if(sobrevivencia, "-")),
    comprimento   = as.numeric(comprimento),

    # A densidade é o tratamento: guardamos as duas versões, porque a ANOVA
    # usa o fator e a regressão (tendência) usa o número.
    densidade_num = as.numeric(densidade),
    densidade     = factor(densidade, levels = c(50, 100, 150, 200)),

    # Variável derivada
    ganho_peso = peso_final - peso_inicial
  ) |>
  # Ordem das colunas: identificação, tratamento, respostas, resto
  select(tanque, densidade, densidade_num, peso_inicial, peso_final,
         ganho_peso, comprimento, sobrevivencia, observacoes)

agua <- agua |>
  mutate(semana = as.integer(semana))

# 3. Conferir. Um resumo rápido pega a maioria dos problemas: NA a mais,
#    valor absurdo, fator com nível que não devia existir.
glimpse(biometria)
summary(biometria)
count(biometria, densidade)     # espera-se 6 tanques por densidade

# 4. Guardar a base tratada em .csv, para quem quiser abrir no Excel ou em
#    outro programa. É entrega, não fonte: o relatório trabalha com os
#    objetos que estão na memória.
dir.create(here("dados", "processados"), showWarnings = FALSE)
write_csv2(biometria, here("dados", "processados", "biometria.csv"))
write_csv2(agua,      here("dados", "processados", "agua.csv"))
```

:::: {.content-visible when-format="html"}
## Exploração

Antes de qualquer teste, olhar. Os chunks `explora-*` produzem resumos e gráficos exploratórios, sem compromisso estético: a ideia é entender a distribuição dos dados, ver se há valores estranhos e ter uma primeira impressão do efeito do tratamento. Esta seção inteira existe só no caderno (HTML): no Word ela é removida, porque um relatório para leitor não mostra exploração.

::: callout-tip
## Como ler esta seção

Cada exploração tem um chunk próprio, e a explicação do que se está procurando fica nos comentários dentro dele. Clique em **Código** para abrir o chunk e leia os comentários antes de olhar o gráfico: eles dizem o que conferir e o que seria sinal de problema.
:::

**Resumo por tratamento.** O primeiro contato com os números: quantos tanques por densidade, média, desvio e intervalo de confiança. Serve para conferir se o delineamento está inteiro (seis tanques por grupo) e se a ordem das médias já sugere o efeito.

```{r}
#| label: explora-resumo
# O que conferir:
#   - n = 6 em todos os grupos (nenhum tanque perdido);
#   - as médias caem com a densidade? Se sim, a ANOVA tende a dar F alto;
#   - desvios parecidos entre grupos (senão, atenção ao teste de Levene).
resumir_grupo(biometria, ganho_peso, densidade)
resumir_grupo(biometria, sobrevivencia, densidade)
```

**Distribuição da resposta por grupo.** O boxplot mostra a forma (simetria, dispersão), e os pontos mostram cada tanque. É o rascunho da ANOVA: se as caixas já não se sobrepõem, o teste só vai confirmar.

```{r}
#| label: explora-ganho
# O que conferir:
#   - caixas de tamanho parecido (variâncias homogêneas);
#   - nenhum ponto muito fora da sua caixa (possível erro de digitação);
#   - a mediana desce com a densidade? É o efeito que a ANOVA vai testar.
# Boxplot + pontos: o boxplot mostra a forma, os pontos mostram quantos
# tanques há e onde cada um caiu.
ggplot(biometria, aes(x = densidade, y = ganho_peso)) +
  geom_boxplot(width = 0.5, outlier.shape = NA, colour = "grey50") +
  geom_jitter(width = 0.1, height = 0, size = 2, alpha = 0.7) +
  labs(x = "Densidade (peixes/m³)", y = "Ganho de peso (g)",
       title = "Exploratório: ganho de peso por densidade") +
  tema_projeto()
```

**Coerência entre variáveis.** Peso e comprimento andam juntos em peixe. Um ponto longe da nuvem quase sempre é erro de digitação, não biologia.

```{r}
#| label: explora-peso-comprimento
# O que conferir:
#   - os pontos formam uma nuvem crescente e apertada?
#   - algum ponto isolado (peso alto com comprimento baixo, ou o contrário)?
#     Se houver, volte à planilha e confira o tanque.
# Serve para pegar erro de digitação: um peixe de 180 g com 10 cm não existe.
# O T14 não tem comprimento (ver o chunk tratar). Preferimos tirá-lo de
# propósito e dizer quantos ficaram de fora, em vez de deixar o ggplot
# descartar em silêncio.
com_comprimento <- filter(biometria, !is.na(comprimento))
message(nrow(biometria) - nrow(com_comprimento), " tanque(s) sem comprimento ficaram fora deste gráfico.")

ggplot(com_comprimento, aes(x = comprimento, y = peso_final)) +
  geom_point(size = 2, alpha = 0.7) +
  geom_smooth(method = "lm", se = FALSE, colour = "grey40", linewidth = 0.6) +
  labs(x = "Comprimento final (cm)", y = "Peso final (g)",
       title = "Exploratório: peso × comprimento") +
  tema_projeto()
```

**Condições do experimento.** A qualidade da água precisa ter sido parecida entre os tratamentos ao longo das semanas. Se um grupo teve oxigênio mais baixo, o efeito da densidade se mistura com o efeito da água, e a ANOVA não separa os dois.

```{r}
#| label: explora-agua
#| fig-height: 5.2
# O que conferir:
#   - as linhas dos quatro tratamentos andam juntas em cada painel?
#   - alguma semana com queda brusca (falha de aeração, chuva)?
#   - oxigênio abaixo de 3 mg/L em algum grupo é sinal de alerta.
# A tabela "agua" está no formato longo (uma linha por tanque × semana).
# Juntamos a densidade de cada tanque e olhamos a média semanal por tratamento.
agua_resumo <- agua |>
  left_join(select(biometria, tanque, densidade), by = "tanque") |>
  pivot_longer(c(temperatura, oxigenio, ph),
               names_to = "variavel", values_to = "valor") |>
  group_by(densidade, semana, variavel) |>
  summarise(media = mean(valor, na.rm = TRUE), .groups = "drop")

ggplot(agua_resumo, aes(x = semana, y = media, colour = densidade)) +
  geom_line(linewidth = 0.7) +
  geom_point(size = 1.8) +
  facet_wrap(~ variavel, scales = "free_y", ncol = 1) +
  scale_colour_manual(values = cores_tratamento, name = "Densidade (peixes/m³)") +
  scale_x_continuous(breaks = 1:8) +
  labs(x = "Semana", y = NULL, title = "Exploratório: qualidade de água") +
  tema_projeto()
```
::::

## Análise dos dados

O ganho de peso de cada tanque foi calculado como a diferença entre o peso médio final e o inicial. A comparação entre densidades usou análise de variância a um fator, seguida do teste de Tukey quando o F foi significativo. Os pressupostos foram verificados nos resíduos do modelo com os testes de Shapiro-Wilk (normalidade) e de Levene (homogeneidade de variâncias) e com os gráficos de diagnóstico. Como os níveis do fator são quantitativos, a tendência do ganho de peso em função da densidade foi resumida por regressão linear. Adotou-se 5 % de significância [@zar2010]. Todas as análises foram feitas em R [@rcore2025].

```{r}
#| label: analisar
#| output: false
# Roteiro clássico do DIC:
#   1. ajustar o modelo (ANOVA a um fator);
#   2. verificar os pressupostos nos resíduos;
#   3. se F for significativo, comparar as médias (Tukey);
#   4. como o fator é quantitativo, olhar também a tendência (regressão).

# 1. Modelo: ganho de peso ~ densidade (fator)
modelo_ganho <- aov(ganho_peso ~ densidade, data = biometria)
tabela_anova <- anova(modelo_ganho)
tabela_anova

# 2. Pressupostos. p > 0,05 em ambos = sem evidência contra os pressupostos.
teste_normalidade <- shapiro.test(residuals(modelo_ganho))
teste_levene      <- leveneTest(ganho_peso ~ densidade, data = biometria)
teste_normalidade
teste_levene

# 3. Comparação de médias (Tukey HSD) e letras de significância:
#    médias com a mesma letra não diferem (p > 0,05).
tukey_ganho <- TukeyHSD(modelo_ganho, "densidade")
tukey_ganho
letras <- multcompLetters(tukey_ganho$densidade[, "p adj"])$Letters

resumo_ganho <- resumir_grupo(biometria, ganho_peso, densidade) |>
  mutate(letra = letras[as.character(densidade)]) |>
  arrange(densidade)
resumo_ganho

# 4. Tendência: a regressão resume o efeito em um único coeficiente
#    (g de ganho a menos por peixe/m³ a mais).
modelo_tendencia <- lm(ganho_peso ~ densidade_num, data = biometria)
summary(modelo_tendencia)

# Números que entram no meio do texto. fmt() e formatar_p() (de R/funcoes.R)
# cuidam da vírgula decimal e das casas.
f_valor   <- fmt(tabela_anova$`F value`[1])
gl_trat   <- tabela_anova$Df[1]
gl_res    <- tabela_anova$Df[2]
p_anova   <- formatar_p(tabela_anova$`Pr(>F)`[1], no_texto = TRUE)
p_shapiro <- formatar_p(teste_normalidade$p.value, no_texto = TRUE)
p_levene  <- formatar_p(teste_levene$`Pr(>F)`[1], no_texto = TRUE)
coef_tend <- abs(coef(modelo_tendencia)[2])
```

:::: {.content-visible when-format="html"}
### Diagnóstico do modelo

A ANOVA só vale se quatro coisas forem verdade. Uma vem do delineamento e as outras três se conferem nos resíduos do modelo. Os gráficos dizem mais que o p-valor dos testes: um p de Shapiro-Wilk pode passar com uma cauda feia, e o gráfico mostra a cauda. Esta seção existe só no caderno; no Word entram só os testes.

::: callout-tip
## Como ler esta seção

Um chunk por pressuposto. Abra o **Código** de cada um: os comentários dizem o que o gráfico deveria mostrar e o que seria sinal de problema.
:::

**1. Independência das observações.** Não se testa; garante-se no delineamento. Aqui, cada tanque-rede é uma unidade experimental sorteada para uma densidade, e a medida é o peso médio do tanque, não o de peixes individuais (que seriam pseudorréplicas). Se isso não fosse verdade, nenhum gráfico abaixo salvaria a análise.

**2. Homogeneidade das variâncias.** Os resíduos devem ter a mesma dispersão em todos os grupos. É o que o teste de Levene mede; o gráfico mostra onde a dispersão cresce.

```{r}
#| label: diagnostico-variancia
#| fig-height: 3.4
# Resíduos × valores ajustados. O que conferir:
#   - a nuvem de pontos tem a mesma altura em todos os grupos (colunas);
#   - a linha vermelha fica perto de zero, sem curva;
#   - um funil (dispersão crescendo com a média) é sinal de variância
#     heterogênea: pense em transformar a resposta ou usar Welch/Kruskal.
plot(modelo_ganho, which = 1)
```

**3. Normalidade dos resíduos.** Não é a variável que precisa ser normal; são os resíduos. É o que o Shapiro-Wilk testa; o gráfico QQ mostra em qual cauda o desvio está.

```{r}
#| label: diagnostico-normalidade
#| fig-height: 3.4
# Gráfico quantil-quantil. O que conferir:
#   - os pontos seguem a linha pontilhada, com pequenos desvios nas pontas;
#   - um "S" ou uma cauda que se descola é sinal de assimetria ou de
#     valor extremo. Com n pequeno (aqui, 24), pequenos desvios são normais.
plot(modelo_ganho, which = 2)
```

**4. Valores influentes.** Um único tanque pode puxar a média do seu grupo e mudar a conclusão. Vale saber quem é.

```{r}
#| label: diagnostico-influencia
#| fig-height: 3.4
# Resíduos padronizados por nível do fator. O que conferir:
#   - a maioria dos pontos entre -2 e 2;
#   - um ponto além de 3 merece uma volta à planilha: erro de digitação ou
#     um tanque que passou por algo diferente (ver a coluna observacoes).
#   Os números ao lado dos pontos são as linhas da base, para você localizar.
plot(modelo_ganho, which = 5)
```
::::

# Resultados

## Descrição dos dados

A @tbl-resumo apresenta o ganho de peso por densidade. A queda das médias com o aumento da densidade já é visível na tabela.

```{r}
#| label: tbl-resumo
#| tbl-cap: "Ganho de peso (g) em 8 semanas por densidade de estocagem. Médias seguidas da mesma letra não diferem pelo teste de Tukey (p > 0,05)."
resumo_ganho |>
  mutate(`IC 95 %` = paste0(fmt(ic_inf, 1), " a ", fmt(ic_sup, 1)),
         across(c(media, dp, ep), ~ fmt(.x, 1))) |>
  select(`Densidade (peixes/m³)` = densidade, n, `Média` = media,
         DP = dp, EP = ep, `IC 95 %`, Tukey = letra) |>
  flextable_ocean()
```

## Análise de variância

A densidade de estocagem afetou o ganho de peso (*F*(`r gl_trat`, `r gl_res`) = `r f_valor`; `r p_anova`; @tbl-anova). Os pressupostos foram atendidos: os resíduos não se afastaram da normalidade (Shapiro-Wilk, `r p_shapiro`) e as variâncias foram homogêneas entre tratamentos (Levene, `r p_levene`).

```{r}
#| label: tbl-anova
#| tbl-cap: "Análise de variância do ganho de peso."
tabela_anova |>
  as.data.frame() |>
  mutate(Fonte = c("Densidade", "Resíduo"),
         GL = Df,
         SQ = fmt(`Sum Sq`, 1),
         QM = fmt(`Mean Sq`, 1),
         F  = fmt(`F value`),
         p  = formatar_p(`Pr(>F)`)) |>
  select(Fonte, GL, SQ, QM, F, p) |>
  # A linha do resíduo não tem F nem p: fica em branco, não "NA" nem "-".
  mutate(across(c(F, p), ~ ifelse(is.na(.x) | .x == "-", "", .x))) |>
  flextable_ocean()
```

A @fig-ganho mostra os tanques individuais, as médias com seus intervalos de confiança e as letras do teste de Tukey.

```{r}
#| label: fig-ganho
#| fig-cap: "Ganho de peso por densidade de estocagem. Pontos: tanques-rede; círculo: média ± IC 95 %; letras iguais indicam médias que não diferem (Tukey, p > 0,05)."
# A figura é feita aqui mesmo: o Quarto numera, legenda e coloca no Word.
ggplot() +
  geom_jitter(data = biometria,
              aes(x = densidade, y = ganho_peso, colour = densidade),
              width = 0.12, height = 0, size = 2, alpha = 0.6) +
  geom_errorbar(data = resumo_ganho,
                aes(x = densidade, ymin = ic_inf, ymax = ic_sup),
                width = 0.2, linewidth = 0.6) +
  geom_point(data = resumo_ganho,
             aes(x = densidade, y = media),
             size = 3, shape = 21, fill = "white", stroke = 1) +
  geom_text(data = resumo_ganho,
            aes(x = densidade, y = ic_sup, label = letra),
            vjust = -0.8, size = 4) +
  scale_colour_manual(values = cores_tratamento, guide = "none") +
  scale_y_continuous(expand = expansion(mult = c(0.05, 0.12))) +
  labs(x = "Densidade de estocagem (peixes/m³)",
       y = "Ganho de peso em 8 semanas (g)") +
  tema_projeto()
```

## Tendência com a densidade

Tratando a densidade como variável contínua, o ganho de peso caiu, em média, `r fmt(coef_tend, 3)` g para cada peixe/m³ a mais — ou seja, cerca de `r fmt(coef_tend * 50, 1)` g a cada 50 peixes/m³ (@fig-tendencia).

```{r}
#| label: fig-tendencia
#| fig-cap: "Relação linear entre densidade de estocagem e ganho de peso. Reta de regressão com IC 95 %."
ggplot(biometria, aes(x = densidade_num, y = ganho_peso)) +
  geom_smooth(method = "lm", colour = "grey30", fill = "grey85", linewidth = 0.7) +
  geom_point(aes(colour = densidade), size = 2, alpha = 0.7) +
  scale_colour_manual(values = cores_tratamento, guide = "none") +
  scale_x_continuous(breaks = c(50, 100, 150, 200)) +
  labs(x = "Densidade de estocagem (peixes/m³)",
       y = "Ganho de peso em 8 semanas (g)") +
  tema_projeto()
```

# Discussão

A queda do ganho de peso com o aumento da densidade é o resultado esperado quando o alimento e o oxigênio passam a ser disputados por mais peixes no mesmo volume, e aparece de forma consistente em experimentos com tilápia em diferentes fases de vida [@elsayed2002]. O que este exemplo acrescenta é a forma da resposta: as duas densidades mais baixas não diferiram entre si, e a perda concentra-se a partir de 150 peixes/m³, o que sugere um limiar, e não uma queda contínua desde o início. A regressão resume a tendência média, mas é o teste de Tukey que localiza onde a diferença começa.

Duas limitações merecem registro. A primeira é que a variável analisada foi o ganho de peso médio por tanque; a produção total por tanque, que cresce com a densidade até certo ponto, pode levar a uma decisão diferente. A segunda é que a sobrevivência, medida mas não analisada aqui, é parte indispensável dessa decisão. Os dados são fictícios e servem para ilustrar o método, não para orientar manejo.

# Conclusão

Nas condições do experimento, densidades maiores reduziram o ganho de peso da tilápia, e a diferença entre a menor e a maior densidade foi de aproximadamente `r fmt(resumo_ganho$media[1] - resumo_ganho$media[4], 0)` g por peixe em oito semanas. A escolha da densidade de trabalho, porém, depende também da produção total por tanque e da sobrevivência, que este exemplo não explora.

# Referências {.unnumbered}

::: {#refs}
:::

:::: {.content-visible when-format="html"}
::: callout-note
## Sobre este documento

Este arquivo é o projeto inteiro: importa a planilha, prepara os dados, explora, analisa e escreve, nesta ordem. O mesmo `.qmd` gera o Word para o leitor e este caderno em HTML, com o código. Para refazer tudo do zero, reinicie o R e clique em Render. Para ver qualquer passo intermediário, rode o chunk correspondente no RStudio.
:::
::::
